1. 선택제어문
1-1. if (조건식) {
실행문;
}
else if (조건식){
실행문;
}
else {
실행문;
}

1-2. switch (정수형 조건식) {

	case (정수형 상수식) : 
			실행문;		
			break;
	default :
			실행문;
			break;
}
*break 문이 없다면, 하나의 case가 실행된 후, 그 아래 모든 case들이 차례로 실행됩니다. 이를 **"fall through"**라고 합니다.

2. 반복제어문
2-1. for (초기식;조건식;증감식) {
	실행문;
}

2-2. while(조건식){
	실행문;
}

2-3. break, continue

3. 함수
함수선언, 함수호출, 함수정의

======
배열 
-배열의 이름은 배열의 인덱스0의 주소이다.

-정수형
널문자 신경 쓸 필요 없다. 
요소의 수보다 초깃값이 적으면 남는 요소는 0으로 초기화된다.
중괄호로 배열의 값을 초기화하면 배열크기를 생략할 수 있다.
정수형 배열의 크기  sizeof(배열명)/sizeof(배열명[0])

-문자열형
sizeof(배열이름)은 배열크기이다. 
널문자를 고려한 배열크기
초기화하지 않는 경우 반드시 널문자도 저장해야 한다.
strcpy()
gets() scanf()
puts() printf()

